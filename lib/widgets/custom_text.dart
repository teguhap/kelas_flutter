import 'package:flutter/material.dart';

class CustomDataText extends StatelessWidget {
  const CustomDataText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Data',
      style: TextStyle(color: Colors.red),
    );
  }
}
